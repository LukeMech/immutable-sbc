# Autologin the baked-in account (05-gnome-minimal.sh) -- no setup wizard, and no
# password to type at the greeter (account is locked), so this reaches the desktop.
[daemon]
AutomaticLoginEnable=True
AutomaticLogin=lm
