# Same "us" layout as /etc/vconsole.conf, for Xwayland/mutter -- what `localectl
# set-x11-keymap` would generate, shipped statically since there's no interactive step.
Section "InputClass"
        Identifier "system-keyboard"
        MatchIsKeyboard "on"
        Option "XkbLayout" "us"
EndSection
