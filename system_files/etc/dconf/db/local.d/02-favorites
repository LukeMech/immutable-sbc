# Full replacement, not an append (dconf can't append) -- the only real
# GUI apps this image ships.
[org/gnome/shell]
favorite-apps=['org.gnome.Console.desktop', 'org.gnome.Nautilus.desktop', 'org.gnome.Settings.desktop']
