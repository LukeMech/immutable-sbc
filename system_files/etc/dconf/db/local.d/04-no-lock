# lm's password is locked, so a locked session can never be unlocked -- must disable
# locking outright: idle-delay=0 alone doesn't stop manual locking (Super+L, confirmed).
[org/gnome/desktop/lockdown]
disable-lock-screen=true

[org/gnome/desktop/screensaver]
lock-enabled=false
